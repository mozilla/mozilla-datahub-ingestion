---
source:
  type: "file"
  config:
    filename: "./recipes/bootstrap/telemetry_platforms.json"

sink:
  type: "datahub-rest"
  config:
    server: ${DATAHUB_GMS_URL}
    token: ${DATAHUB_GMS_TOKEN}
